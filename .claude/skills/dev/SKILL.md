---
name: dev
description: 개발 서버를 시작합니다 (port 3000)
disable-model-invocation: true
allowed-tools: Bash(pnpm dev)
---

개발 서버를 시작합니다.

1. `pnpm dev` 명령어를 백그라운드로 실행
2. 서버가 시작되면 http://localhost:3000 에서 확인 가능하다고 안내
